services:
  - type: web
    name: seo-ai-suite-v1
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: SERPER_API_KEY
        sync: false
